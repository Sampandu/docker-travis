sudo: required
services:
  - docker

before_install:
  - docker build -t sampandu/docker-travis -f Dockerfile.dev .

script:
  - docker run -e CI=true sampandu/docker-travis npm run test -- --coverage
